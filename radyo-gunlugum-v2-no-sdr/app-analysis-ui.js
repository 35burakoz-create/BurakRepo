(()=>{
const R=window.R;if(!R||R.__analysisUI384)return;R.__analysisUI384=true;const $=s=>document.querySelector(s),esc=v=>R.esc?R.esc(v):String(v??'');
function count(rows,pick){const map=new Map();for(const row of rows||[]){const key=pick(row)||'Belirsiz';map.set(key,(map.get(key)||0)+1)}return[...map.entries()].sort((a,b)=>b[1]-a[1])}
function bars(el,items){if(!el)return;const max=Math.max(1,...(items||[]).map(x=>x[1]));el.innerHTML=items?.length?items.slice(0,12).map(([key,value])=>`<div class="bar-row"><span>${esc(key)}</span><div class="bar-track"><div class="bar-fill" style="width:${value/max*100}%"></div></div><b>${value}</b></div>`).join(''):'<p class="hint">Henüz veri yok.</p>'}
function render(){const logs=R.logs||[],signal=logs.filter(x=>x.signal_strength),avg=signal.length?(signal.reduce((sum,x)=>sum+Number(x.signal_strength),0)/signal.length).toFixed(1):'—',cards=[['Ortalama sinyal',avg],['Ses örneği',logs.filter(x=>x.audio_path).length],['QSL gönderildi',logs.filter(x=>['sent','received'].includes(x.qsl_status)).length],['Konum',new Set(logs.map(x=>x.location).filter(Boolean)).size]];if($('#analysisCards'))$('#analysisCards').innerHTML=cards.map(([label,value])=>`<div class="stat"><b>${value}</b><small>${esc(label)}</small></div>`).join('');bars($('#bandBars'),count(logs,x=>x.band));bars($('#langBars'),count(logs,x=>x.language));bars($('#stationBars'),count(logs.filter(x=>x.station),x=>x.station));const grouped=new Map();for(const x of signal){const hour=Number(String(x.time||'00:00').slice(0,2)),key=`${x.band}|${hour}`,z=grouped.get(key)||{band:x.band,hour,sum:0,n:0};z.sum+=Number(x.signal_strength);z.n++;grouped.set(key,z)}const predictions=[...grouped.values()].map(x=>({...x,avg:x.sum/x.n})).sort((a,b)=>b.avg-a.avg||b.n-a.n).slice(0,6);if($('#predictions'))$('#predictions').innerHTML=predictions.length?predictions.map(x=>`<div class="prediction"><b>${esc(x.band)} · ${String(x.hour).padStart(2,'0')}:00–${String((x.hour+1)%24).padStart(2,'0')}:00</b><br>Ortalama sinyal ${x.avg.toFixed(1)}/5 · ${x.n} kayıt</div>`).join(''):'<p class="hint">Sinyal puanlı kayıtlar biriktikçe kişisel tahmin burada oluşacak.</p>'}
R.renderAnalysis=render;R.analysisUI={render,count};R.router?.register?.('analysis',{enter:()=>setTimeout(render,0)});R.events?.on?.('data:loaded',()=>{if(R.router?.current?.()==='analysis')render()});R.features?.register?.('analysis-ui',{ready:true,provider:'app-analysis-ui'});
})();