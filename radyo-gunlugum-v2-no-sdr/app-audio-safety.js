(()=>{
const R=window.R;if(!R||R.__audioSafety376)return;R.__audioSafety376=true;const $=s=>document.querySelector(s);const C=globalThis.RADIO_APP_CONFIG||{timezone:'Europe/Istanbul'};const TZ=C.timezone||'Europe/Istanbul';const esc=v=>R.esc?R.esc(v):String(v??'');const toast=v=>R.toast?R.toast(v):alert(v);
function duration(){const d=Number($('#audioPreview')?.duration);return Number.isFinite(d)&&d>0?Math.round(d):null}
function closeModal(){$('#appAudioOverlay')?.remove();$('#appAudioModal')?.remove()}
function modal(title,html){closeModal();const o=document.createElement('div');o.id='appAudioOverlay';o.className='foundation-diag-overlay';o.onclick=closeModal;const m=document.createElement('section');m.id='appAudioModal';m.className='foundation-diag';m.innerHTML=`<div class="foundation-diag-head"><div><span>SES GÜVENLİĞİ</span><h2>${esc(title)}</h2></div><button class="foundation-diag-close" type="button">×</button></div><div id="appAudioModalBody" style="display:grid;gap:8px;margin-top:14px">${html}</div>`;document.body.append(o,m);m.querySelector('.foundation-diag-close').onclick=closeModal;return m}
function closestRecentLog(ts,maxMin=30){const t=new Date(ts||Date.now()).getTime();let best=null;for(const x of R.logs||[]){const z=new Date(x.created_at||`${x.date}T${String(x.time||'').slice(0,5)}:00+03:00`).getTime(),d=Math.abs(z-t);if(!best||d<best.diff)best={log:x,diff:d}}return best&&best.diff<=maxMin*60000?best:null}
function goNew(path){R.reset?.();R.fill?.('audioPath',path);if($('#formSuggestionStrip'))$('#formSuggestionStrip').textContent='Ses dosyası bu yeni kayda bağlanmaya hazır. Kaydı kaydettiğinde ilişkilendirilecek.';R.router?.go?.('log',{source:'audio-recovery'});$('#tab-log')?.classList.add('v38-form-open');window.scrollTo({top:0,behavior:'smooth'})}
async function attach(path,log){if(!log||log.audio_path)return toast(log?.audio_path?'Bu kayıtta zaten ses var.':'Kayıt bulunamadı.');const p={audio_path:path,source:'audio'},d=duration();if(d)p.audio_duration_seconds=d;const q=await R.S.from('radio_logs').update(p).eq('id',log.id).eq('user_id',R.me.id);if(q.error)return toast(q.error.message);R.fill?.('audioPath','');await R.load?.();toast(`Ses “${log.station||R.freq?.(log)||'kayıt'}” kaydına bağlandı.`)}
async function listOrphans(){if(!R.me)return[];const q=await R.S.storage.from('radio-audio').list(R.me.id,{limit:200,sortBy:{column:'created_at',order:'desc'}});if(q.error)throw q.error;const refs=new Set((R.logs||[]).map(x=>x.audio_path).filter(Boolean));return(q.data||[]).map(x=>({...x,path:`${R.me.id}/${x.name}`})).filter(x=>!refs.has(x.path))}
async function recovery(){let orphans=[];try{orphans=await listOrphans()}catch(error){return toast('Ses dosyaları kontrol edilemedi: '+error.message)}const html=orphans.length?orphans.map((x,i)=>{const c=closestRecentLog(x.created_at,30),size=x.metadata?.size?`${Math.round(x.metadata.size/1024)} KB`:'';return`<div class="foundation-health-row" style="align-items:flex-start"><div><b>Bağsız ses ${i+1}</b><span style="display:block;margin-top:3px;color:var(--ds-muted,#64748b);font-size:11px">${esc(new Date(x.created_at).toLocaleString('tr-TR',{timeZone:TZ}))}${size?' · '+size:''}</span>${c?`<span style="display:block;margin-top:3px;color:var(--ds-muted,#64748b);font-size:11px">Olası kayıt: ${esc(c.log.station||R.freq?.(c.log)||'kayıt')} · ${Math.round(c.diff/60000)} dk fark</span>`:''}</div><div class="row-actions"><button class="btn ghost" data-audio-play="${i}">Dinle</button>${c&&!c.log.audio_path?`<button class="btn primary" data-audio-attach="${i}">Bağla</button>`:''}<button class="btn ghost" data-audio-new="${i}">Yeni kayıt</button></div></div>`}).join(''):'<div class="foundation-health-row ok"><b>Bağsız ses yok</b><span>Arşiv tutarlı</span></div>';const m=modal('Ses Kurtarma',html);m.addEventListener('click',async e=>{const p=e.target.closest('[data-audio-play]');if(p){const x=orphans[Number(p.dataset.audioPlay)],s=await R.S.storage.from('radio-audio').createSignedUrl(x.path,600);if(s.error)return toast(s.error.message);window.open(s.data.signedUrl,'_blank');return}const a=e.target.closest('[data-audio-attach]');if(a){const x=orphans[Number(a.dataset.audioAttach)],c=closestRecentLog(x.created_at,30);if(c){await attach(x.path,c.log);closeModal()}return}const n=e.target.closest('[data-audio-new]');if(n){const x=orphans[Number(n.dataset.audioNew)];closeModal();goNew(x.path)}})}
function installRecoveryButton(){const card=$('#tab-audio .smart-card');if(!card||$('#appRecoverAudio'))return;const b=document.createElement('button');b.id='appRecoverAudio';b.className='btn ghost';b.style.marginTop='10px';b.textContent='Bağsız sesleri kontrol et';b.onclick=recovery;card.appendChild(b)}
function showUploadedActions(pathOverride){const msg=$('#audioMsg'),path=pathOverride||$('#audioPath')?.value;if(!msg||!path)return;let box=$('#appAudioActions');if(!box){box=document.createElement('div');box.id='appAudioActions';box.className='row-actions';box.style.marginTop='8px';msg.after(box)}const editId=$('#logId')?.value,recent=closestRecentLog(Date.now(),30);box.innerHTML=`<button class="btn primary" data-audio-new-upload>Yeni kayda bağla</button>${editId?'<button class="btn ghost" data-audio-edit-return>Düzenlenen kayda dön</button>':recent&&!recent.log.audio_path?`<button class="btn ghost" data-audio-recent="${recent.log.id}">Son kayda bağla</button>`:''}`;box.querySelector('[data-audio-new-upload]')?.addEventListener('click',()=>goNew(path));box.querySelector('[data-audio-edit-return]')?.addEventListener('click',()=>{R.router?.go?.('log',{source:'audio-upload'});$('#tab-log')?.classList.add('v38-form-open')});box.querySelector('[data-audio-recent]')?.addEventListener('click',()=>attach(path,(R.logs||[]).find(x=>String(x.id)===String(recent.log.id))))}
R.openAudioRecovery=recovery;R.audioSafety={listOrphans,recovery,attach,showUploadedActions};R.events?.on?.('audio:uploaded',x=>showUploadedActions(x?.path));R.events?.on?.('auth:changed',x=>{if(x?.authenticated)setTimeout(installRecoveryButton,0)});R.events?.on?.('render:all',installRecoveryButton);R.events?.on?.('route:changed',x=>{if(x?.to==='audio'&&$('#audioPath')?.value)setTimeout(()=>showUploadedActions(),0)});installRecoveryButton();R.features?.register?.('audio-safety',{ready:true,provider:'app-audio-safety'});
})();