(()=>{
const R=window.R;if(!R||R.__foundation37)return;R.__foundation37=true;
const C=globalThis.RADIO_APP_CONFIG||{version:'3.7.0',displayVersion:'V3.7',timezone:'Europe/Istanbul',locale:'tr-TR',origin:{name:'Bozköy, Torbalı, İzmir',lat:38.151,lon:27.36}};
const $=s=>document.querySelector(s),norm=v=>R.norm?R.norm(v):String(v??'').toLocaleLowerCase('tr-TR'),esc=v=>R.esc?R.esc(v):String(v??'');
function loadCss(){if(document.querySelector('link[data-foundation-css]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='app-foundation.css';l.dataset.foundationCss='1';document.head.appendChild(l)}
loadCss();
const listeners=new Map();
const events={on(name,fn){if(typeof fn!=='function')return()=>{};if(!listeners.has(name))listeners.set(name,new Set());listeners.get(name).add(fn);return()=>events.off(name,fn)},once(name,fn){const off=events.on(name,(...args)=>{off();fn(...args)});return off},off(name,fn){listeners.get(name)?.delete(fn)},emit(name,payload){for(const fn of [...(listeners.get(name)||[])]){try{fn(payload)}catch(error){console.error('Event listener failed',name,error)}}}};
R.events=events;
function parts(date=new Date()){const p=Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone:C.timezone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).formatToParts(date).filter(x=>x.type!=='literal').map(x=>[x.type,x.value]));return{year:+p.year,month:+p.month,day:+p.day,hour:+p.hour,minute:+p.minute,second:+p.second,date:`${p.year}-${p.month}-${p.day}`,time:`${p.hour}:${p.minute}`,monthKey:`${p.year}-${p.month}`}}
const clock={timezone:C.timezone,locale:C.locale,parts,now:()=>new Date(),today:()=>parts().date,time:()=>parts().time,month:()=>parts().monthKey,minutes:()=>{const p=parts();return p.hour*60+p.minute},local:()=>{const p=parts();return{date:p.date,time:p.time,month:p.monthKey}},format:(date=new Date(),options={})=>new Intl.DateTimeFormat(C.locale,{timeZone:C.timezone,...options}).format(date)};
R.clock=clock;R.today=clock.today;R.local=clock.local;
const DIAG_KEY='radio-diagnostics-v37';
function readErrors(){try{return JSON.parse(localStorage.getItem(DIAG_KEY)||'[]')}catch{return[]}}
let errors=readErrors(),lastToastAt=0;
function serializeError(error,source='runtime'){const e=error instanceof Error?error:new Error(typeof error==='string'?error:JSON.stringify(error));return{at:new Date().toISOString(),source,message:e.message||String(error),stack:String(e.stack||'').slice(0,1800),route:R.router?.current?.()||null,version:C.version}}
function reportError(error,source='runtime',{silent=false}={}){const item=serializeError(error,source);errors=[item,...errors].slice(0,20);try{localStorage.setItem(DIAG_KEY,JSON.stringify(errors))}catch{};console.error(`[${source}]`,error);events.emit('diagnostics:error',item);if(!silent&&R.me&&R.toast&&Date.now()-lastToastAt>60000){lastToastAt=Date.now();R.toast('Bir sorun algılandı. Menü > Sistem Durumu bölümünden ayrıntıları görebilirsin.')}return item}
window.addEventListener('error',e=>{if(e.error||e.message)reportError(e.error||e.message,'window.error',{silent:true})});window.addEventListener('unhandledrejection',e=>reportError(e.reason||'Unhandled promise rejection','unhandledrejection',{silent:true}));R.reportError=reportError;
const legacySwitch=typeof R.switch==='function'?R.switch.bind(R):null;
const knownRoutes=new Set(['home','now','log','audio','analysis','map','calendar','qsl','guide','smart','atlas','ai','memory','propagation']);const routeHooks=new Map();
function hashRoute(){const h=decodeURIComponent(location.hash||'').replace(/^#/,'').replace(/^tab=/,'');return h&&knownRoutes.has(h)?h:null}function visibleRoute(){const a=[...document.querySelectorAll('.tab-view')].filter(v=>!v.classList.contains('hidden'));return a.length===1?a[0].id.replace(/^tab-/,''):null}
let current=hashRoute()||R.uiState?.tab||visibleRoute()||'home';
const router={register(name,hooks={}){if(!name)return;knownRoutes.add(name);routeHooks.set(name,hooks||{});return()=>routeHooks.delete(name)},has(name){return knownRoutes.has(name)||!!document.getElementById(`tab-${String(name)}`)},current:()=>current,go(name,meta={}){let target=String(name||'home');if(!router.has(target)){reportError(new Error(`Unknown route: ${target}`),'router',{silent:true});target='home'}const from=current,before={from,to:target,source:meta.source||'app'};events.emit('route:before',before);try{routeHooks.get(from)?.leave?.(before);const out=legacySwitch?.(target);current=visibleRoute()||target;routeHooks.get(current)?.enter?.({...before,to:current});events.emit('route:changed',{from,to:current,source:before.source});return out}catch(error){reportError(error,'router');if(target!=='home'){try{legacySwitch?.('home');current='home'}catch{}}events.emit('route:error',{from,to:target,error});return undefined}},sync(){const next=hashRoute()||visibleRoute()||R.uiState?.tab||current;if(next&&next!==current){const prev=current;current=next;events.emit('route:changed',{from:prev,to:current,source:'browser'})}return current}};
R.router=router;R.switch=name=>router.go(name,{source:'R.switch'});window.addEventListener('popstate',()=>setTimeout(()=>router.sync(),0));window.addEventListener('hashchange',()=>setTimeout(()=>router.sync(),0));
function addMap(map,key,value){if(!key)return;if(!map.has(key))map.set(key,[]);map.get(key).push(value)}
function buildIndex(logs,guides){const stations=new Map(),countries=new Map(),frequencies=new Map(),docs=[];for(const x of logs){const station=String(x.station||x.smart_station||'').trim(),sk=norm(station),countryParts=String(x.country||'').split(/[/,;]/).map(v=>v.trim()).filter(Boolean),fk=x.band&&x.frequency!=null?`${x.band}|${Number(x.frequency).toFixed(3)}`:'';if(sk)addMap(stations,sk,x);for(const c of countryParts)addMap(countries,norm(c),x);if(fk)addMap(frequencies,fk,x);docs.push({kind:'log',id:x.id,text:norm([station,x.country,x.language,x.program,x.notes,x.transcript,x.smart_station,x.smart_program,x.band,x.frequency].join(' ')),raw:x})}for(const x of guides)docs.push({kind:'guide',id:x.id,text:norm([x.station,x.country,x.language_content,x.frequency,x.content_hint,x.time_text,x.band,x.mode].join(' ')),raw:x});return{stations,countries,frequencies,docs}}
const store={state:{logs:[],schedules:[],guideEntries:[],updatedAt:0},index:buildIndex([],[]),sync(reason='sync'){store.state={logs:Array.isArray(R.logs)?R.logs:[],schedules:Array.isArray(R.schedules)?R.schedules:[],guideEntries:Array.isArray(R.guideEntries)?R.guideEntries:[],updatedAt:Date.now()};store.index=buildIndex(store.state.logs,store.state.guideEntries);events.emit('store:updated',{reason,...store.counts()});return store.state},counts(){return{logs:store.state.logs.length,schedules:store.state.schedules.length,guideEntries:store.state.guideEntries.length,stations:store.index.stations.size,countries:store.index.countries.size,frequencies:store.index.frequencies.size}},search(query,limit=80){const q=norm(String(query||'').trim());if(!q)return[];return store.index.docs.filter(d=>d.text.includes(q)).slice(0,limit)}};
R.store=store;store.sync('foundation-boot');const legacyLoad=typeof R.load==='function'?R.load.bind(R):null;if(legacyLoad)R.load=async(...args)=>{events.emit('data:loading');try{const out=await legacyLoad(...args);store.sync('R.load');events.emit('data:loaded',store.counts());return out}catch(error){reportError(error,'R.load');events.emit('data:error',error);throw error}};
const featureMap=new Map();R.features={register(name,meta={}){featureMap.set(name,{name,...meta});events.emit('feature:registered',{name,...meta})},has:name=>featureMap.has(name),get:name=>featureMap.get(name),list:()=>[...featureMap.values()]};for(const [name,test] of [['search',()=>typeof R.openGlobalSearch==='function'],['memory',()=>typeof R.openRadioMemory==='function'],['propagation',()=>!!document.querySelector('#tab-propagation')||typeof R.renderPropagation==='function'],['router',()=>!!R.router],['store',()=>!!R.store],['clock',()=>!!R.clock]])if(test())R.features.register(name,{ready:true});
function health(){const swSupported='serviceWorker'in navigator;const checks=[['Router',!!R.router,'router'],['Store',!!R.store,'store'],['Saat servisi',!!R.clock,'clock'],['Global arama',typeof R.openGlobalSearch==='function','search'],['Radio Memory',typeof R.openRadioMemory==='function','memory'],['Propagation',!!document.querySelector('#tab-propagation'),'propagation'],['Rehber',Array.isArray(R.guideEntries)&&R.guideEntries.length>0,'guide'],['Service Worker',swSupported&&!!navigator.serviceWorker.controller,'sw']];return checks.map(([label,ok,key])=>({label,key,ok,state:ok?'Hazır':key==='sw'&&swSupported?'Bekliyor':'Eksik'}))}
function snapshot(){return{version:C.version,displayVersion:C.displayVersion,online:navigator.onLine,route:router.current(),authenticated:!!R.me,store:store.counts(),features:R.features.list(),serviceWorker:{supported:'serviceWorker'in navigator,controlled:!!navigator.serviceWorker?.controller},errors:errors.slice(0,10),time:new Date().toISOString()}}
function closeDiagnostics(){document.querySelector('#foundationDiagOverlay')?.remove();document.querySelector('#foundationDiag')?.remove()}
function openDiagnostics(){closeDiagnostics();const o=document.createElement('div');o.id='foundationDiagOverlay';o.className='foundation-diag-overlay';o.onclick=closeDiagnostics;const m=document.createElement('section');m.id='foundationDiag';m.className='foundation-diag';const h=health(),snap=snapshot();m.innerHTML=`<div class="foundation-diag-head"><div><span>SİSTEM DURUMU · ${esc(C.displayVersion)}</span><h2>Foundation Diagnostics</h2></div><button class="foundation-diag-close" type="button">×</button></div><div class="foundation-health">${h.map(x=>`<div class="foundation-health-row ${x.ok?'ok':x.key==='sw'?'warn':'bad'}"><b>${esc(x.label)}</b><span>${esc(x.state)}</span></div>`).join('')}</div><div class="foundation-diag-block"><h3>Özet</h3><pre class="foundation-diag-pre">${esc(JSON.stringify({...snap,errors:undefined},null,2))}</pre></div><div class="foundation-diag-block"><h3>Son hatalar (${errors.length})</h3><pre class="foundation-diag-pre">${esc(errors.length?errors.map(x=>`${x.at} · ${x.source}\n${x.message}`).join('\n\n'):'Kayıtlı hata yok.')}</pre></div><div class="row-actions" style="margin-top:14px"><button class="btn ghost" data-foundation-copy>Tanı bilgisini kopyala</button><button class="btn ghost" data-foundation-clear>Hata geçmişini temizle</button></div>`;document.body.append(o,m);m.querySelector('.foundation-diag-close').onclick=closeDiagnostics;m.querySelector('[data-foundation-clear]').onclick=()=>{errors=[];try{localStorage.removeItem(DIAG_KEY)}catch{};openDiagnostics()};m.querySelector('[data-foundation-copy]').onclick=async()=>{const text=JSON.stringify(snapshot(),null,2);try{await navigator.clipboard.writeText(text);R.toast?.('Tanı bilgisi kopyalandı.')}catch{console.log(text)}}}
R.diagnostics={health,snapshot,open:openDiagnostics,clear(){errors=[];try{localStorage.removeItem(DIAG_KEY)}catch{}}};R.openDiagnostics=openDiagnostics;
document.addEventListener('click',e=>{if(e.target.closest('[data-foundation-diagnostics]')){e.preventDefault();openDiagnostics();return}if(e.target.closest('[data-v38menu]'))setTimeout(()=>{const sheet=$('#v38Sheet');if(!sheet||sheet.querySelector('[data-foundation-diagnostics]'))return;const b=document.createElement('button');b.type='button';b.className='foundation-menu-row';b.dataset.foundationDiagnostics='1';b.innerHTML='<span><b>Sistem Durumu</b><small>Router, veri, arama, PWA ve hata tanıları</small></span><i>›</i>';sheet.querySelector('.v38-menu-footer')?.before(b)},0)},true);
R.version=C.version;R.displayVersion=C.displayVersion;document.title=`Radyo Günlüğüm ${C.displayVersion}`;const ver=$('.topbar h1 span');if(ver)ver.textContent=C.displayVersion;const sub=$('.topbar .sub');if(sub)sub.textContent='Foundation — daha sağlam navigasyon, ortak veri durumu ve tanı altyapısı.';events.emit('foundation:ready',{version:C.version,route:router.current(),store:store.counts()});
})();
